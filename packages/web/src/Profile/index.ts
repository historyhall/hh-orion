import {Profile} from "./Profile";
import {Page} from "../types";

export default {
    menu: {
        path: '/profile',
        icon: 'user',
        position: 'right',
    },
    component: Profile
} as Page