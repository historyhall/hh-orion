{
  "name": "hh-orion-domain",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index",
  "scripts": {
    "build": "tsc --build && yarn tsc ",
    "start": "tsc --build --watch && yarn tsc ",
    "test:deps": "depcheck",
    "test:lint": "eslint",
    "test:types": "tsc"
  },
  "dependencies": {
    "@elastic/elasticsearch": "^8.14.0",
    "@mikro-orm/core": "^6.3.2",
    "bcrypt": "^5.1.1",
    "debug": "^4.3.6",
    "hh-orion-schema": "0.1.0",
    "jsonwebtoken": "^9.0.2",
    "typescript": "^5.5.4",
    "uuid": "^10.0.0"
  },
  "publishConfig": {
    "directory": "dist"
  }
}
