import {Page} from "../types";
import {Profile} from "./Profile";

export default {
    menu: {
        icon: 'user',
        position: 'right',
    },
    path: '/profile',
    component: Profile
} as Page