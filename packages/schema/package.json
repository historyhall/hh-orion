{
  "name": "hh-orion-schema",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build && yarn tsc ",
    "start": "tsc --build --watch && yarn tsc ",
    "test:deps": "depcheck",
    "test:lint": "eslint",
    "test:types": "tsc"
  },
  "dependencies": {
    "typescript": "^4.9.5"
  }
}
