import {Profile} from "./Profile";
import {Page} from "../types";

export default {
    menu: {
        icon: 'user',
        position: 'right',
    },
    path: '/profile',
    component: Profile
} as Page